# PostgreSQL/NeonDB Connection
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# Express Session
SESSION_SECRET="your_strong_session_secret_here"

# Encryption Key for sensitive configuration data (32 bytes hex string)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY="your_32_byte_hex_encryption_key_here"

# Google OAuth Credentials (for Google Calendar Integration)
# Obtain these from Google Cloud Console
GOOGLE_CLIENT_ID="your_google_client_id"
GOOGLE_CLIENT_SECRET="your_google_client_secret"
# If using a fixed redirect URI for Docker/production, uncomment and set this
# GOOGLE_REDIRECT_URI="http://localhost:5000/api/oauth/google/callback"

# Evolution API (for WhatsApp Integration)
# These are examples; refer to your Evolution API provider documentation
EVOLUTION_API_URL="your_evolution_api_base_url" # e.g., http://localhost:8080
EVOLUTION_API_KEY="your_evolution_api_key_or_instance_name" # This varies by Evolution API setup
EVOLUTION_INSTANCE="your_evolution_instance_name_or_api_key" # This varies by Evolution API setup

# Variables for docker-compose (defaults are often fine if not overridden)
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your_postgres_password_for_docker_compose # IMPORTANT: Set this for local Docker
# POSTGRES_DB=appointments

# Application Port (already set in Dockerfile and docker-compose, but can be listed for clarity)
# PORT=5000
